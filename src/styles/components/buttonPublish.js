import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  addPostBtn: {
    borderWidth: 0,
    borderRadius: 100,
    paddingVertical: 16,
    alignItems: "center",
  },
});

export default styles;
